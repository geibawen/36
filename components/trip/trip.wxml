<template name="m-article">
  <view class="m-trip" bindtap="viewTrip" data-id="{{ article.id }}" data-name="{{ article.name }}">              
    <image class="cover" src="http://ow47de8h7.bkt.clouddn.com/{{ article.image }}" background-size="cover"></image>
    <view class="info">
      <text class="h2">{{ article.title }}</text>
      <view class="detail">
        <view><text>{{ }}</text><text>{{  }}</text></view>
        <view class="place" wx:if="{{ 0 }}">{{  }}</view>
      </view>
      <view class="user" wx:if="{{ mode === 1 }}">
        <image class="avatar" src="{{  }}"></image>
        <view class="name"> {{  }}</view>
      </view>
      <view class="bottom" wx:if="{{ 0 }}"></view>
    </view>
  </view>
</template>